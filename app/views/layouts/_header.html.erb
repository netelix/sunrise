<nav class="navbar-main navbar navbar-expand-lg navbar-light bg-white">
  <%= link_to(deferred_image_tag('logo.png'), root_path, class: 'navbar-brand') %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="navbar-nav ml-auto">
      <% if current_user %>
        <%= render('layouts/logged_user_nav') %>
      <% else %>
        <li class="nav-item">
          <%= link_to t('.sign_in'), login_path, data: {modal: true}, class: "nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>