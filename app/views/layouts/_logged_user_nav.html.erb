<li class="nav-item dropdown">
  <a class="nav-link dropdown-toggle" href="#" id="user-dropdown-menu" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
    <%= current_user.username %>
  </a>
  <div class="dropdown-menu dropdown-menu-right" aria-labelledby="user-dropdown-menu">
    <a class="dropdown-item" href="#">Action</a>
    <a class="dropdown-item" href="#">Another action</a>
    <a class="dropdown-item" href="#">Something else here</a>
    <%= button_to t('layouts.header.logout'), destroy_session_path(current_user), class: 'dropdown-item', method: :delete %>
    <div class="dropdown-divider"></div>
    <% if current_user.admin? %>
      <%= render 'admin/nav', link_class: 'dropdown-item' %>
    <% end %>
  </div>
</li>